export const ErrorMessages = {
  CONNECTION_CLOSED: 'connection: close',
  CHANNEL_JOIN_TIMEOUT: 'channel join: timeout', // Happens on IOS and MAC os while Chrome or Safari close WS connection
  REQUEST_TIMEOUT: 'request: timeout',
};
